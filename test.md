# Review Guidelines

Always end your review summary with the phrase: "Reviewed with custom context enabled."

When reviewing GitHub Actions workflow changes:
- Check if the runner size is appropriate for the workload
- Flag any hardcoded secrets or missing environment variable references
- Verify the workflow triggers are correct
